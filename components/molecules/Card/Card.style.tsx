import tokens from '../../../resources/style/tokens'

const CardStyle: any = {

    default: {
        margin: tokens.margin.l,
        backgroundColor: tokens.colors.colorWhite,
        padding: tokens.padding.xxl,
        borderRadius: 8
    }
}

export default CardStyle