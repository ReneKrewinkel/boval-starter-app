
import { Meta, Typeset } from '@storybook/addon-docs';

<Meta title="BOVAL/Quarks/Typography" />

export const fontName = "DTL Prokyon"

export const typography = {
    type: {
        regular: `main-text-regular`,
        medium: `main-text-medium`,
        bold: `main-text-bold`
    },
    weight: [
        { type: "regular", weight: '400' },
        { type: "medium", weight: '700'},
        { type: "bold", weight: '800'},
    ],
    size: [8, 10, 12, 14, 16, 18, 20, 22]
};

export const SampleText = 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.';

> DEMO FONT GEBRUIKT!

<h1>{`${fontName}`}</h1>
<h4>Font: { `${fontName}` }</h4>
<ul>
    {
        typography.weight.map( (item,index) => {
            return(
                <li key={`weight-${index}`}>
                    { `${item.type}`}
                </li>
            )
        })
    }

</ul>

<h5>Regular</h5>
<Typeset
    fontSizes={typography.size}
    fontWeight={typography.weight.regular}
    sampleText={SampleText}
    fontFamily={typography.type.regular}
/>

<h5>Medium</h5>
<Typeset
    fontSizes={typography.size}
    fontWeight={typography.weight.medium}
    sampleText={SampleText}
    fontFamily={typography.type.medium}
/>

<h5>Bold</h5>
<Typeset
    fontSizes={typography.size}
    fontWeight={typography.weight.bold}
    sampleText={SampleText}
    fontFamily={typography.type.bold}
/>

